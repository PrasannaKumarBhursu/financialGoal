import React from 'react';
import GaugeProp from '../features/gauge/GaugeProp';


export default function Cardstyles(props) {
    return (
        <GaugeProp percent='0.3' score="Vulnerable" message="Your financials need immediate attention and course correction." />

    )
}

